// SPDX-License-Identifier: PolyForm-Noncommercial-1.0.0

// Horizontal step (px) a row indents per nesting level — a sub-item sits this
// much further right than its parent, so the tree shape reads at a glance.
// Shared by the row, the composer, and the drag ghost so the ghost's checkbox
// lines up exactly with the row it will land beside (or one step in, nested).
export const INDENT_PER_LEVEL = 22;
