---
type: Added
title: i18n module
---

An `i18n` module: `createI18n` builds a typed, dependency-free `t()` runtime over your per-language catalogs, with code-split on-demand language loading, a `LanguageRoot` that mirrors the preference, gates first paint, and tracks `<html lang>`, and `setLanguage` to switch at runtime.
