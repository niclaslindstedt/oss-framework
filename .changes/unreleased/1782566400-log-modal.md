---
type: Added
title: LogModal
---

A `LogModal` in the `logging` module: a modal that shows the step-by-step log of a single operation — passed entries (`{ ts, level, text }`), an injectable header glyph, and English-default labels — opened on demand from a status line that needs explaining, as the focused counterpart to `LogViewer`.
