---
type: Fixed
title: Inline edit fields scroll clear of the soft keyboard
---

An `InlineEditField` that mounts low on screen on a touch device — a new
checklist row typed below the last, or the bottom add-item composer — now
scrolls itself into view once the keyboard settles, instead of appearing hidden
behind it.
