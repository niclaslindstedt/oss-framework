---
type: Added
title: CipherGlyph busy indicator
---

`components`: a `CipherGlyph` — an "encryptish" busy indicator (a short run of monospace cipher glyphs that continuously re-scramble) used in place of a spinner for work in flight, honouring reduce-motion both ways (the OS preference and the theme engine's `data-reduce-motion` attribute).
