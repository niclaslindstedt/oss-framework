---
type: Added
title: Markdown module
---

A live-preview Markdown editor (`MarkdownEditor`, the `./markdown` subpath) that formats every line except the one the caret sits on, plus the dependency-free parser it renders from (`classifyLines` / `parseInline` / `shortenUrl`) and a `NoteIcon` glyph.
