---
type: Added
title: GlyphPicker defaultIcon
---

`GlyphPicker` now accepts a `defaultIcon` node for its leading "clear" cell, so an app whose glyph-less default isn't the catalogue's `folder` — including one that varies by entity kind — can make the cell show the same mark clearing actually reverts to, instead of always the fallback glyph.
