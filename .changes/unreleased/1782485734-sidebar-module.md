---
type: Added
title: Sidebar module
---

A `sidebar` module: `Sidebar` is the responsive navigation shell — a draggable floating button and swipe-dismissable drawer on phones, a permanent docked sidebar on wide screens — that takes its content as `children`, with the `useDraggableMenuButton`, `useDrawerSwipeClose`, and `useSidebarInset` helpers it builds on.
