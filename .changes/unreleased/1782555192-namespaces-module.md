---
type: Added
title: Namespaces module
---

A `namespaces` module: the `Namespace` data shape, pure list operations (create/rename/restyle/remove, connect-time merge, slugify), a favicon resolver that re-badges the tab to the active namespace, and a presentational `NamespacesModal` for managing workspaces; ships alongside a new `ConfirmDialog` component.
