---
type: Added
title: Logging module
---

A `logging` module: `createLogStore` builds an in-app log ring buffer with an optional, debounced localStorage capture mirror and a pub/sub layer, and hands out scoped loggers that double as the `storage` adapter sink.
