---
type: Added
title: Namespace switcher
---

A `NamespaceSwitcher` in the `namespaces` module: the in-drawer collapsible namespace section — tap a row to switch the active namespace, the cog opens the manager, and each other row doubles as a cross-namespace drop target wired to your `sidebar` drag-and-drop.
