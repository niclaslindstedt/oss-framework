---
type: Added
title: Colour mixer
---

The new `color` module ships `ColorMixer` — the saturation/value field and hue
strip every picker uses — over `hexToHsv` / `hsvToHex` / `normalizeHex` /
`withAlpha` / `contrastingInk`.
