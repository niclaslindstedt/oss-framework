---
type: Added
title: Trophy button row form
---

`TrophyButton` gains a `showLabel` prop that renders it as a full-width menu row (label beside the glyph, badge inline) so it drops in among sidebar footer rows, alongside the existing icon-only button.
