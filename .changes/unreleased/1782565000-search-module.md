---
type: Added
title: Search module
---

A `search` module: a pure progressive-query matcher (`compileQuery` / `searchItems` — substring → fuzzy → wildcard → `/regex/`) with character-range scoring, a `Highlighted` renderer, and a generic `SearchModal` overlay that owns the field and empty states while your app supplies the corpus and result rows.
