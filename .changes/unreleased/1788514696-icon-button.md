---
type: Added
title: IconButton
---

A square glyph button with the ARIA a hand-rolled one gets wrong: `aria-pressed`
for a toggle, `aria-haspopup` + `aria-expanded` for a disclosure, and neither
unless you ask.
