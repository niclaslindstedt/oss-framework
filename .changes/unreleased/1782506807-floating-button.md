---
type: Added
title: FloatingButton
---

`sidebar`: a `FloatingButton` component — the draggable, edge-resting round FAB the `Sidebar` floats for its menu toggle, now exported on its own so an app can pin a second global action (open Settings, a composer) from the same primitive, each instance resting and dragging independently.
