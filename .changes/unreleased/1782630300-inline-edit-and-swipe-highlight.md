---
type: Changed
title: Inline edit caret control and drop highlight
---

`InlineEditField` gains `selectOnFocus` (caret-at-end instead of select-all), an `onBackspaceEmpty` handoff, and a commit-source argument; `SwipeableRow` gains a `highlighted` prop that paints a drop-target overlay above its opaque foreground.
